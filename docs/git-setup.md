
# Git Setup Log — ibrahim-yasin

**Program:** AI.SPIRE Pre-Work
**Date completed:** 23/2/2026
**GitHub username:** ibrahim-yasin

---

## Git Identity

Run `git config --list` in your terminal and fill in your configured values:

| Config key | Your value |
|------------|------------|
| `user.name` |ibrahim yasin |
| `user.email` |ibrayasin02@outlook.com |
| `core.autocrlf` |true |

---
## Setup
## Platform

| Field | Your value |
|-------|------------|
| Operating system | (windows 10 pro) |
| Terminal used | (Git Bash) |
| Git version (`git --version`) |2.53.0|

---

## Authentication

- [github_pat_] Personal Access Token created in GitHub Settings
- [ ] PAT stored securely (password manager or secure notes)
- [ ] Credential caching confirmed — Git does not re-prompt on push

---

## Clone Verification

After cloning, output of `git status` from inside your repo:



